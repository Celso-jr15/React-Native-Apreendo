import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    Contacts: {
        flex: 1,
        padding: 10,
        backgroundColor: '#fff',
        alignItems: "center",              }
});

export default styles